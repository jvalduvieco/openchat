[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
test = "pipenv run python -m unittest discover -s tests -t ."

[dev-packages]
flake8 = "*"
parameterized = "*"

[packages]

[requires]
python_version = "3.7"
