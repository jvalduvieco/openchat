[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
test = "pipenv run python -m unittest discover -s tests -t ."
start_dev = "pipenv run python -m apps.RESTAPI"

[dev-packages]
flake8 = "*"
parameterized = "*"

[packages]
Flask = "*"
flask_injector = "*"

[requires]
python_version = "3.7"

[pipenv]
allow_prereleases = true
